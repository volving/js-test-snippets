var arr=[1,3,4,5]
var add;
//add.prototype.isSingle = false;
add = function(argument){
	var sum=0;
	if (arguments.length === 1) {


	if (this.prototype.isSingle) {
		sum += this.prototype.isSingle + argument;
		this.prototype.isSingle = false;
	}
	}	

	for(var i=0;i<argument.length;i++){
		//alert(argument[i]);
		sum=sum+argument[i];
	}
	return console.log(sum);
}
add(arr)

